{
  "name": "stellarium-skycultures",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Found-in-Space/stellarium-skycultures.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node ./scripts/build-packages.js",
    "build:western": "node ./scripts/build-packages.js western",
    "changeset": "changeset",
    "examples:build": "node ./scripts/build-examples.js",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "release:checked": "npm run build && npm test && changeset publish",
    "test": "node ./scripts/smoke-test.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
